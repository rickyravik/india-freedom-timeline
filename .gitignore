node_modules
dist
*.local
.DS_Store
shots.mjs
.wrangler
.dev.vars
.playwright-mcp
test-results
playwright-report
.lighthouseci

# Impeccable session artifacts — keep only the DESIGN.md sidecar
.impeccable/*
!.impeccable/design.json
